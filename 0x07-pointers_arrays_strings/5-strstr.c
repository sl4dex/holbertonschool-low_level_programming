/**
 * . - 
 * @
 *
 * Return: .
 */
char *_strstr(char *haystack, char *needle)
{
	int key, pos, offset;

	offset = 0;

	for (pos = 0; haystack[pos] != 0; pos++)
	{
		if (s[pos] != needle[key - offset])
		{
			offset++
		}
}

