/**
 * _strlen - returns the length of a string.
 * @s: pointer to string (array of chars)
 *
 * Return: String size
 */
int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
	}
	return (i);
}
