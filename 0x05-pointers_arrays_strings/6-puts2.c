#include "main.h"

/**
 * puts2 - prints every other character of a string
 * @str: string 
 */
void puts2(char *str)
{
	int i;

	for (i = 0; str[i] != 0; i++)
	{
	}
	for (i = 0; str[i] <= i; i += 2)
	_putchar(10);
}
